import java.util.Scanner;

public class Reggie {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);

        String userInput = safeinput.getRegExString(in, "Match the regular expression pattern.");
    }
}